# This workflow will build and push a Python application to an Azure Web App when a commit is pushed to your default branch.
#
# This workflow assumes you have already created the target Azure App Service web app.
# For instructions see https://docs.microsoft.com/en-us/azure/app-service/quickstart-python?tabs=bash&pivots=python-framework-flask
#
# To configure this workflow:
#
# 1. Download the Publish Profile for your Azure Web App. You can download this file from the Overview page of your Web App in the Azure Portal.
#    For more information: https://docs.microsoft.com/en-us/azure/app-service/deploy-github-actions?tabs=applevel#generate-deployment-credentials
#
# 2. Create a secret in your repository named AZURE_WEBAPP_PUBLISH_PROFILE, paste the publish profile contents as the value of the secret.
#    For instructions on obtaining the publish profile see: https://docs.microsoft.com/azure/app-service/deploy-github-actions#configure-the-github-secret
#
# 3. Change the value for the AZURE_WEBAPP_NAME. Optionally, change the PYTHON_VERSION environment variables below.
#
# For more information on GitHub Actions for Azure: https://github.com/Azure/Actions
# For more information on the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# For more samples to get started with GitHub Action workflows to deploy to Azure: https://github.com/Azure/actions-workflow-samples

name: Build and deploy Python app to Azure Web App  # The name of the workflow

env:
  AZURE_WEBAPP_NAME: "mlapps"  # set this to the name of your Azure Web App
  PYTHON_VERSION: '3.10'              # set this to the Python version to use
  AZURE_RESOURCE_GROUP_NAME: "Azuredevops" # Set this to your Azure Resource Group
  AZURE_REGION: "West Europe" # Set this to your Azure Region
  AZURE_PLAN: "mlappPlan" # Set this to your Azure App Service Plan name

on:
  push:
    branches: 
      - master
#  workflow_dispatch:     
permissions:
  contents: read
jobs:
  build:
    name: Build Python app
    runs-on: ubuntu-latest
    #if: ${{ github.event.workflow_run.conclusion == 'success' }}

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python version
        uses: actions/setup-python@v4
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'

      - name: Create and start virtual environment
        run: |
          make venv
          source ~/.udacity-devops/bin/activate

      - name: Install dependencies
        run: make install

  deploy:
    name: Deploy to Azure Web App
    runs-on: ubuntu-latest
    needs: build
    environment: 'production_environment'
    steps:
      - uses: actions/checkout@v4
      # Log in to Azure
      - uses: azure/login@v2
        with:
          creds: ${{secrets.AZURE_CREDENTIALS}}

      # deploy web App      
      - name: 'Deploy to Azure Web App'
        run: |
          az webapp up \
            --name "${{ env.AZURE_WEBAPP_NAME }}" \
            --resource-group "${{env.AZURE_RESOURCE_GROUP_NAME}}" \
            --runtime "PYTHON:${{ env.PYTHON_VERSION }}" \
            --plan "${{ env.AZURE_PLAN }}" \
            --sku F1 \
            --location "${{ env.AZURE_REGION }}" 
        